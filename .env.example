# API keys (rellena solo las que uses)
GROQ_API_KEY=
DEEPSEEK_API_KEY=
OPENROUTER_API_KEY=

# Ollama (por defecto en localhost)
OLLAMA_API_BASE=http://localhost:11434

# Workspace para ejecucion de shell (sandbox)
BRIDGE_WORKSPACE=./workspace
